export const greatings = () : string => {
  return 'Hello World';
};

export const age : number = 22;

// Default export

const farewell = () : string => {
  return 'Goodbye World';
};

export default farewell;